spring:
  datasource:
    url: jdbc:mysql://db:3306/dinnervery?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    username: dinner
    password: dinnerpwd
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
server:
  port: 8080
